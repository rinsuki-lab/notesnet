## 頻出コマンド

- `pnpm dev:refresh`: OpenAPIやGraphQLのクライアント側型定義を更新する。GraphQLのクエリを書いた後にクライアント側コードで型エラーが出る場合はこれを呼ぶ。
- `pnpm lint`: 型チェック・スタイルチェックを含めたlintが走る。
- `pnpm fix`: 上記のlintが走るが、修正可能な部分は自動で修正される。これを実行した後にファイルを編集する場合は再度ファイルを読み直すこと。

