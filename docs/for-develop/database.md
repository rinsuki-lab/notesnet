## データベースについて

- 現在のスキーマは `schema.sql` に書いてあります
- スキーマに変更を加えた場合、`./generate_migrate.py` でマイグレーションを生成できます
  - 生成したマイグレーションは `sqlx migrate run` で実行できます
- Rust からデータベースは `sqlx::query!("クエリ", パラメータ, ……)` でクエリできます
